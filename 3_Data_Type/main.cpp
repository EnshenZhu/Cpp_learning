#include <iostream>

using namespace std;

int main() {
    char grade = 'A'; // represent a single character
    string phrase = "Giraffe Academy"; // represnet a bundle of characters
    int age = -50; // integer
    float value1;
    double gpa = 2.3; // a double data type can store more decimal numbers than a float
    bool isMale = true;

    cout << grade << endl;

    return 0;
}
